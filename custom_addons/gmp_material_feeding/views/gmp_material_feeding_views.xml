<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Action -->
    <record id="action_gmp_material_feeding" model="ir.actions.act_window">
        <field name="name">Cấp / nạp liệu chế biến</field>
        <field name="res_model">gmp.material.feeding</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu -->
    <!-- <menuitem id="menu_noodle_production_log_root" name="Nhật ký sản xuất mì" sequence="10"/> -->
    <menuitem
        id="menu_gmp_material_feeding"
        name="Cấp / nạp liệu chế biến"
        parent="gmp_weighing_monitoring.menu_noodle_production_log_root"
        action="action_gmp_material_feeding"
        sequence="2"
    />

    <!-- Tree view -->
    <record id="view_gmp_material_feeding_tree" model="ir.ui.view">
        <field name="name">gmp.material.feeding.tree</field>
        <field name="model">gmp.material.feeding</field>
        <field name="arch" type="xml">
            <tree>
                <field name="log_datetime"/>
                <field name="log_date" optional="hide"/> 
                <field name="productionplan_id" optional="hide"/>
                <field name="productionplancode"/>
                <field name="productionplanname" optional="hide"/>
                <field name="productionplanfactory" optional="hide"/>
                <field name="line_id" optional="hide"/>
                <field name="linecode" optional="hide"/>
                <field name="linename" optional="hide"/>
                <field name="shift_id" optional="hide"/>
                <field name="shiftcode" optional="hide"/>
                <field name="shiftname" optional="hide"/>
                <field name="item_id" optional="hide"/>
                <field name="itemcode" optional="hide"/>
                <field name="itemname" optional="hide"/>
                <field name="fromts" optional="hide"/>
                <field name="tots" optional="hide"/>
                <field name="material_id" optional="hide"/>
                <field name="materialcode"/>
                <field name="materialname"/>
                <field name="bom_quantity" optional="hide"/>
                <field name="actual_quantity" optional="hide"/>
                <field name="quantity_variance" optional="hide"/>
                <field name="cip" optional="hide"/>
                <field name="cop" optional="hide"/>
                <field name="plan_quantity_status" optional="hide"/>
                <field name="bom_quantity_status" optional="hide"/>
                <field name="order_quantity_status" optional="hide"/>
                <field name="cleanliness_status" optional="hide"/>
                <field name="operating_status" optional="hide"/>
                <field name="cross_contamination" optional="hide"/>
                <field name="final_result"/>
                <field name="operator" optional="hide"/>
                <field name="note"/>
                <field name="allowed_item_ids" optional="hide"/>
                <field name="allowed_material_ids" optional="hide"/>
            </tree>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_gmp_material_feeding_form" model="ir.ui.view">
        <field name="name">gmp.material.feeding.form</field>
        <field name="model">gmp.material.feeding</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="log_datetime"/>
                        <field name="log_date" invisible="1"/> 
                        <field name="productionplan_id" 
                            context="{'search_default_u_docdate': log_date}"
                            domain="[('u_docdate', '=', log_date)]"
                            options="{'no_create': True, 'no_open': True}"/>
                        <field name="productionplancode"/>  
                        <field name="productionplanname"/>
                        <field name="productionplanfactory"/>
                        <field name="line_id"/>
                        <field name="linecode"/>
                        <field name="linename"/>
                        <field name="shift_id"/>
                        <field name="shiftcode"/>
                        <field name="shiftname"/>
                        <field name="item_id" options="{'no_open': True}"/>
                        <field name="itemcode"/>
                        <field name="itemname"/>                        
                        <field name="fromts"/>
                        <field name="tots"/>
                        <field name="material_id"/>
                        <field name="materialcode"/>
                        <field name="materialname"/>
                        <field name="uom_id" options="{'no_open': True}"/>
                        <field name="uomcode"/>
                        <field name="uomname"/>                     
                    </group>

                    <group string="Cấp/nạp liệu chế biến">
                        <field name="bom_quantity"/>
                        <field name="actual_quantity"/>
                        <field name="quantity_variance"/>
                    </group>

                    <group string="Thiết bị, công cụ, dụng cụ">
                        <field name="cip"/>
                        <field name="cop"/>
                    </group>

                    <group string="Cấp liệu theo">
                        <field name="plan_quantity_status"/>
                        <field name="bom_quantity_status"/>
                        <field name="order_quantity_status"/>
                    </group>

                    <group string="Khu vực và thao tác cấp liệu">
                        <field name="cleanliness_status"/>
                        <field name="operating_status"/>
                        <field name="cross_contamination"/>
                    </group>

                    <group>
                        <field name="final_result"/>
                        <field name="operator"/>
                        <field name="note"/>
                        <field name="allowed_item_ids" invisible="1"/>
                        <field name="allowed_material_ids" invisible="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
